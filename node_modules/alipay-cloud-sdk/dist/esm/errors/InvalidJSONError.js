import { CloudSDKError } from './CloudSDKError';
import { ErrorCode } from '../constants/Error';
export function InvalidJSONError() {
    return new CloudSDKError({
        errCode: ErrorCode.INVALID_JSON_OBJECT,
        errMsg: 'JSON 解析错误',
    });
}
