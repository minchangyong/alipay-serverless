import { CloudSDKError } from './CloudSDKError';
import { ErrorCode } from '../constants/Error';
export function FeatureNotSupportError() {
    throw new CloudSDKError({
        errCode: ErrorCode.FEATURE_NOT_SUPPORT,
        errMsg: '暂时不支持',
    });
}
