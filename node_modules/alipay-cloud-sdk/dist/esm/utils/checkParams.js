import { CloudSDKError } from '../errors/CloudSDKError';
import { getUndefinedParams } from './getUndefinedParams';
import { ErrorCode } from '../constants/Error';
export function checkParams(params, values) {
    var undefinedParams = getUndefinedParams(params, values);
    if (undefinedParams.length > 0) {
        throw new CloudSDKError({
            errCode: ErrorCode.NOT_FOUND_PARAM,
            errMsg: "\u53C2\u6570\u7F3A\u5931\uFF0C\u8BF7\u68C0\u67E5: " + undefinedParams.join()
        });
    }
    return true;
}
