export function get(obj, fn, defaultValue) {
    try {
        var result = fn(obj);
        return result === void 0 ? defaultValue : result;
    }
    catch (err) {
        return defaultValue;
    }
}
