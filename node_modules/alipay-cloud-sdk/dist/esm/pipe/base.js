import { OpenCode } from '../constants/OpenCode';
export var base = {
    convertUploadResponse: function (res) {
        if (res.success && res.result && res.result.code === OpenCode.SUCCESS) {
            var result = res.result;
            return {
                success: res.success,
                requestId: res.requestId,
                result: {
                    code: result.code,
                    msg: result.msg,
                    fileId: result.imageId,
                    fileUrl: result.imageUrl,
                },
            };
        }
        return res;
    }
};
