export var mini = {
    convertQueryParams: function (queryParam) {
        var keys = Object.keys(queryParam) || [];
        var values = keys.map(function (key) {
            var value = queryParam[key];
            if (value === void 0) {
                return '';
            }
            return key + "=" + encodeURIComponent(value);
        });
        return values.join('&');
    },
};
